export interface Contact {
    id: number;
    name: string;
    description: string;
    photo: string;
}

export interface NewContact {
    name: string;
    description: string;
    photo: string;
}